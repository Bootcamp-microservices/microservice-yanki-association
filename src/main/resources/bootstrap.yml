eureka:
  instance:
    prefer-ip-address: true
spring:
  application:
    name: microservice-yanki-association
  cloud:
    config:
      enabled: true
      label: main
      uri: http://localhost:7000